"""InfluAudit ML service.

Cold-start fraud, engagement-pod and comment-quality estimation. There is no
labeled training data yet, so every capability here is unsupervised or
rule-based and every response is explicitly marked as an estimate.
"""
